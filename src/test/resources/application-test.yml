spring:
  datasource:
    hikari:
      connection-timeout: 1000
      login-timeout: 1
  jpa:
    show-sql: true
db-state-check:
  unavailability:
    delay-ms: 5000

scheduler:
  time:
    write:
      enabled: false
      delay: PT1S

hazelcast:
  mode: LOCAL
  instance-name: hazelcast-instance
